import { createAction, handleActions } from 'redux-action';

import { Map } from 'immutable';
import { pender } from 'redux-pender';

// action types

// action creators

// initial state
const initaionState = Map({});

// render
export default handleActions({}, initaionState);
